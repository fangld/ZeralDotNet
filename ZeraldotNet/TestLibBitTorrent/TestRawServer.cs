using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using NUnit.Framework;
using ZeraldotNet.LibBitTorrent;
using System.Threading;
using System.Net;

namespace ZeraldotNet.TestLibBitTorrent
{
    [TestFixture]
    public class TestRawServer
    {
        /// <summary>
        /// Starting side close
        /// </summary>
        [Test]
        public void Test1()
        {
            Flag fa = new Flag();
            Flag fb = new Flag();
            try
            {
                //Encrypter handlerA = new Encrypter();
                //RawServer serverA = new RawServer(fa, 100, 100, false);
                
                //Encrypter handlerB = new Encrypter();
                //RawServer serverB = new RawServer(fb, 100,100, false);

                //Thread.Sleep(500);
                //SingleSocket ca = sa.StartConnect(new IPEndPoint(IPAddress.Parse("127.0.0.1"), 6801), null);
                //Thread.Sleep(5000);

                //SingleSocket cb = handlerB.m
            }

            catch
            {
            }
        }
    }
}
